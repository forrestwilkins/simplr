<% if @shared_items.present? %>
  var shared_items = $("#shared_items");

  var anchor = $("#shared_items_anchor");

  shared_items.remove();

  anchor.before("<%=j render 'stacked' %>");

  window.navigator.vibrate(1);
<% end %>
