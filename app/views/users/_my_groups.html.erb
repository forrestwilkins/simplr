<%= link_to (user.eql?(current_user) ? my_groups_path : their_groups_path(user)) do %>
  <table align="center">
    <tr>
      <% for group in user.my_groups.reject{|g| g.image_url.nil? }.sample 4 %>
        <td>
          <%= image_tag group.image_url, class: :my_groups_group_icon %>
        </td>
      <% end %>
    </tr>
  </table>
<% end %>
